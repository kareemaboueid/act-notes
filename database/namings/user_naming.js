/**
 * ### User database structure names
 * @description User database structure names for model and collection.
 */
const user_db_naming = {
  /** @name M(model) - `User` */
  M: String('User'),
  /** @name C(collection) - `users` */
  C: String('users'),
};

export default user_db_naming;
