/**
 * ### Note database structure names
 * @description Note database structure names for model and collection.
 */
const note_db_naming = {
  /** @name M(model) - `Note` */
  M: String('Note'),
  /** @name C(collection) - `notes` */
  C: String('notes'),
};

export default note_db_naming;
